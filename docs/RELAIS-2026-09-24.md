# Relais du 24 septembre 2026 — passage sur le cloud

Note laissée à la demande de Cyril, avant de déplacer le travail sur le cloud.
État : tout est commité, fusionné dans `main` et en ligne (production sur `4bc5c25`,
PR #135). Aucune branche ni PR en attente.

## Ce qui a été livré le 24 septembre (PR #125 à #135)

- **Surtitres interdits** (règle permanente) : `DESIGN.md` « Surtitres », `AGENTS.md`,
  `tests/no-eyebrows.test.cjs`, `node scripts/detect-eyebrows.cjs <url>`.
- **Fil d'Ariane** rendu par le serveur partout, sans flash (#128).
- **Accueil** : vraies nouveautés, variées par marque (#129, `lib/home-rails.js`).
- **Filtres « Filtrer et trier »** sur toutes les listes du menu (#130, #132, #133, #135) :
  sous-catégories, 7 familles (dont Jardin et Assises), catalogue complet, marques,
  gammes, créateurs (`/produits.html?designer=`) et sélections (Nouveautés…).
  Filtre « Catégorie » sur les familles, le catalogue, les marques et les créateurs.
  Pages numérotées de 60. Détails : `docs/CATALOG_FILTERS.md`, `docs/FAMILY_PAGES.md`.
- **Index commun** (`lib/catalog-index.js`) : 4 049 fiches lues en ~1 min, servies au
  CDN en 5 parties (`/index-catalogue/<partie>.json`, 15 min + renouvellement en
  arrière-plan). Le CDN refuse une réponse de 18 Mo, d'où l'allègement et le découpage.
  Sans index (≈ 1 min après chaque mise en ligne), les pages reprennent leur liste
  d'origine, jamais gardée au CDN.
- **Shopify** : lectures réessayées 2 fois après une coupure (#131, `lib/shopify/client.js`) ;
  mutations jamais rejouées.
- **Pages écrites par le serveur** (#133 à #135), demande « hardcoder ce qu'il y a à
  hardcoder » :
  - `#site-data` dans chaque page : menu, marques, promotions, réglages du méga menu,
    version (plus d'appels `/api/menu`, `/api/brands`, `/api/build`, `/api/promos`) ;
  - cartes produit complètes dès le serveur (bouton compris), plus de reconstruction ;
  - fiche produit complète (`v3/pdp-view.mjs`, partagé serveur/navigateur) ;
  - nuancier Fermob complet (`v3/nuancier-view.mjs`) ;
  - panier : état choisi avant le premier affichage ;
  - plus d'apparition au défilement (`.reveal`).
  - Audit production : 0 appel, 0 reconstruction, 0 saut sur les pages principales.
    Outil : `node scripts/audit-loading.mjs` (voir l'en-tête du script).

## À faire ensuite

1. **Page Promotions** : dernière page encore lue et reconstruite par le navigateur
   (`/api/collection/promotions/products`). La faire passer sur l'index et le rendu
   serveur en respectant les règles « Promotions Shopify » d'`AGENTS.md` (variante
   remisée, tag `promotion`, collection, stock, lien, panier) et
   `node --test tests/promotion-variants.test.cjs tests/promotion-variants-api.test.cjs`.
   Les collections `promo-*` sont aussi hors du rendu filtré (`lib/filter-scopes.js`).
2. **Décision à confirmer avec Cyril** : une famille = sa collection Shopify **plus**
   ses sous-catégories du menu (la collection Assises ne contenait aucun des 712 canapés,
   Tables aucune table basse). Assises passe de 256 à 923 fiches. Retour possible :
   `memberSources` dans `lib/filter-scopes.js`, ou corriger les collections dans Shopify.
3. **Données Shopify** : la collection « Tables de café » ne contient que des articles
   Bistro d'extérieur (chaises, coussins, carafe) ; la page est vide (déjà le cas avant).
4. **Livraison** : grille au poids validée (6,90 / 9,90 / 19,90 / 70 / 120 €) mais bloquée :
   2 663 fiches actives sans poids. Tarif actuel : 50 € unique.
5. **En attente de Cyril** : mise en page de la page Marques (≈ 10 marques mises en avant
   puis A–Z), taille du badge de remise, tag « Icône » (il s'en charge), identifiants de
   l'app Admin pour la newsletter (`SHOPIFY_ADMIN_CLIENT_ID` / `SECRET`).
6. **Mobile, plus tard** : accordéons du pied de page, hauts de listes plus courts.
7. **Petit reste** : sur la fiche, le badge d'offre automatique sonde encore
   `/api/cart/preview` à l'affichage (volontairement dynamique).

## Points pratiques

- Tests obligatoires après une modification de données, routes ou pagination :
  `node --test tests/family-pages.test.cjs tests/table-collections.test.cjs tests/product-specs.test.mjs`
  (la suite complète : `node --test tests/*.test.*`, 162 tests au 24 septembre).
- Vérifier chaque changement visuel sur ordinateur, puis à 390 et 360 px, et lancer
  `node scripts/detect-eyebrows.cjs <preview>` avant publication.
- Previews Vercel : un déploiement depuis un dépôt git dont l'auteur n'est pas membre
  de l'équipe Vercel reste bloqué (TEAM_ACCESS_REQUIRED). Méthode utilisée en local :
  copier `git ls-files` + `.vercel/project.json` dans un dossier sans `.git`, puis
  `npx vercel deploy <dossier> --target=preview --archive=tgz`, et supprimer la copie.
  La production se déploie à la fusion dans `main`.
- Plan Vercel Hobby : surveiller le temps de calcul (reconstructions de l'index).
